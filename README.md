# Nomisma

Personal finance tracker — accounts, transactions, budgets, and recurring reminders.

## Stack

- **TanStack Start** — full-stack React framework with SSR
- **Convex** — real-time backend and database
- **Better Auth** — authentication with Google OAuth
- **shadcn/ui** — UI components and utilities
- **Tailwind CSS v4** — styling

## Scripts

```bash
pnpm dev
pnpm build
pnpm lint
pnpm typecheck
pnpm check
pnpm convex:dev
pnpm convex:codegen
```

`pnpm check` is the baseline verification command.

## Environment

Required environment variables:

- `CONVEX_DEPLOYMENT`
- `VITE_CONVEX_URL`
- `VITE_CONVEX_SITE_URL`
- `SITE_URL`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`

## Generated Files

- `convex/_generated/*` — generated by Convex
- `src/routeTree.gen.ts` — generated by TanStack Start

Do not edit generated files by hand.

## Local Setup

1. Install dependencies with `pnpm install --frozen-lockfile`.
2. Copy `.env.example` to a local env file and fill in the required values.
3. Start Convex with `pnpm convex:dev`.
4. Start the app with `pnpm dev`.

## License

This work is distributed under the [GNU General Public License 3.0 (GPL)](license) License.

See [license](license) for more information.
